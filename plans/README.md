# Implementation Plans

Generated by the improve skill on 2026-07-15, from a standard-depth audit at
commit `dcecd22`. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

**Repo-wide note for executors**: at planning time the working tree carried a
staged, uncommitted package rename `optimiser/` → `kairos/`. All plans use the
`kairos/` paths (the working-tree state). Each plan's drift check explains how
to treat the rename. The verification command for everything is
`.venv/bin/python -m pytest -q` (172 passing at planning time, ~6s).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Render Saturday sessions in both week grids | P1 | S | — | DONE |
| 002  | Guard priority-list rebuild against Highlighted re-entrancy | P2 | S | — | DONE |
| 003  | Cache pairing_impossibility on AppState | P2 | S | — | DONE |
| 004  | Single source of truth for slot_sig and fmt_clock | P3 | S | 003 (soft) | DONE |
| 005  | Precompute per-choice scoring fragments (slim `compute_raw`) | P1 | L | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All four plans are logically independent, but **003 and 004 both edit
  `kairos/tui/state.py` and `kairos/tui/app.py`** — execute sequentially in
  numbered order (004 locates its edits by symbol, so it survives 003's line
  shifts).
- 001 and 002 touch disjoint files and can run in parallel with anything.
- **005** (`kairos/scoring.py`, `kairos/search.py`) is independent of 001–004
  and can run in parallel; it is the highest-leverage perf item (targets the
  73%-of-retune scoring pass). From the 2026-07-15 focused **performance
  audit** — see "Performance audit findings" below for the sibling items not
  yet planned.

## Performance audit findings (2026-07-15, focused perf pass at `dcecd22`)

Measured against the real `config.yaml` (59,748 clash-free combos): one retune
≈ 2.2 s, of which `score_raw` is 1.6 s (73%). Weight-slider drags take ~570 ms.
Leverage-ordered:

- **P1 — defer breakdown dicts** (`weight_scored`/`rank`): build 6-key
  breakdown dicts only for the ~55 displayed combos, not all 59,748. Measured
  211 ms → 63 ms on the weight pass (70% off every slider tick). Effort M,
  risk M. *Not yet planned.*
- **P2 — cache `Session.online` / `Choice.footprint`** on the frozen
  dataclasses (recomputed 2.67M / 736K×/pass). Effort S, risk LOW. *Not yet
  planned.*
- **P3 — flat difficulty map** per scoring pass (`config.difficulty()` called
  734K×/pass). Effort S, risk LOW. P2+P3 together measured ~11% off retune.
  *Not yet planned.*
- **P4 — slim `compute_raw` via per-choice fragments** → **planned as 005**.
- **P5 — clash matrix for `enumerate_clashfree`** (O(1) rep-pair lookups):
  only helps the 712 ms space-change path. Effort M, risk LOW-MED. *Not yet
  planned.*
- Complementary (non-algorithmic): run `retune` on a Textual worker thread with
  slider debounce (audit finding #14) so the UI never blocks — the biggest
  *perceived* win; pairs with P1.

## Audit findings not selected this round (not rejected — available for a future run)

From the 2026-07-15 audit table (see the conversation/audit record):

- #1 CI workflow, #2 ruff/mypy, #3 AGENTS.md, #4 pin Textual + `_Static__content` fragility (the recommended "top 5 bundle" minus test gaps)
- #5 module-code validation (share URL → cache path), #6 config.yaml value validation (incl. `top_n <= 0`), #7 NUSMods JSON shape validation
- #8 test gaps: HTTP-200 fetch/cache-write path; `e` ballot-export keybinding
- #12 criterion/preference metadata registry, #14 slider debounce + worker, #15 enumeration cap (investigate-first), #16 config.yaml schema docs, #17 unify the two week renderers
- Direction options D1 (`run` adopts arrangements), D2 (`kairos lock` subcommand), D3 (`run --export/--json`)

## Deferred minors from the lockable-non-balloted-slots feature (2026-07-22, merged `19b0090`)

Raised by per-task or whole-branch review, triaged as non-blocking. None affects
behaviour; the first two are the ones with real (if small) value.

- **`prepare_groups` warning text is stale** (`kairos/search.py:43-46`): says "no fixed
  choice" when `locked` is now the key both writers produce. It also fires *inside the
  running TUI* whenever a user unlocks a non-balloted group — a newly reachable path,
  since you could not unlock a `fixed` lecture before. Harmless (Textual 8.2.8 wraps the
  run loop in `redirect_stdout`), but both misworded and invisible. Reword to
  "no fixed/locked choice".
- **Post-migration errors name a key the user's file lacks** (`kairos/search.py:36`): if a
  migrated class number no longer exists (timetable changed between semesters),
  `prepare_groups` raises `... (config 'locked') does not exist` while the user's
  `config.yaml` said `fixed`. Diagnosability regression on an error path.
- **`test_selectable_groups_excludes_group_collapsing_to_one_slot_sig` asserts only
  absence**: would pass vacuously if the DELTA fixture's group construction broke. Wants a
  positive precondition (`len(choices) == 2` and one distinct `slot_sig`).
- **The `selectable_groups` ordering assertion does not discriminate**
  (`tests/test_tui_state.py`): it sorts on `(module, abbrev)` while the implementation
  sorts on `(module, lesson_type)`. Verified there are **zero** pairs in `LESSON_ABBREV`
  where the two orders diverge, so the test cannot fail today whichever key is used.
- **The `fixed`+`locked` same-key collision path is untested**: manually exercised during
  review and correct (`fixed` wins the overwrite, balloted entries untouched), but not
  pinned against regression.
- **Multi-session rows flatten the session→venue mapping**: a class meeting Mon@A and
  Wed@B renders `@A/B`. Accepted consequence of the "show all distinct venues" decision
  (the alternative printed labels that were false for non-representative classes). Watch
  for label truncation in the Timeslots pane during UAT.

## Findings considered and rejected

- Full detail-pane re-render on timeslot-preview navigation: bounded to a
  single arrangement, cheap — not worth doing.
- Splitting the 387-line `KairosApp` "god object": no current pain; revisit
  only if the TUI grows another major pane.
- Recorded design tradeoffs treated as settled, not findings: raw-cache
  reweight, arrangement-structure cache, impossible-pairing-counts-satisfied,
  weight-0-disables-warnings, lunch penalty −2/day, `free_days` counting
  Mon–Fri only (see `docs/superpowers/specs/`).
